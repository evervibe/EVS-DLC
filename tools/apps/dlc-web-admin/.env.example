# Environment Variables for DLC Web Admin
# Next.js 15 + React 19

# Next app origin (for server-side absolute URLs)
APP_ORIGIN=http://localhost:5174

# DLC API base URL (backend Nest/Fastify app)
DLC_API_BASE=http://localhost:30089

# Name of auth cookie set by /login
AUTH_COOKIE_NAME=dlc_token

# API Configuration (for client-side)
NEXT_PUBLIC_API_URL=http://localhost:30089

# Application Configuration
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_APP_VERSION=1.4.1

# Optional: Enable debug mode
# NEXT_PUBLIC_DEBUG=true

# Optional: Custom health endpoints (auto-derived from API_URL if not set)
# NEXT_PUBLIC_API_HEALTH_URL=http://localhost:30089/health
# NEXT_PUBLIC_REDIS_HEALTH_URL=http://localhost:30089/ops/redis
# NEXT_PUBLIC_DB_HEALTH_URL=http://localhost:30089/ops/db
